/* eslint-disable react/prop-types */
import React from 'react';

export default function Frontend({ stackStateFront }) {
  return (
    <div className={`frontend-fixed ${stackStateFront}`}>Frontend</div>
  );
}
