/* eslint-disable react/prop-types */
import React from 'react';

export default function Backend({ stackStateBack }) {
  return (
    <div className={`backend-fixed ${stackStateBack}`}>Backend</div>
  );
}
